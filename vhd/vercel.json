{
    "version": 2,
    "routes": [
        { "src": "/_next/(.*)", "dest": "/_next/$1" },
        { "src": "/(.*)", "dest": "/" }
    ],
    "builds": [
        { "src": "/package.json", "use": "@vercel/next" }
    ]
  }