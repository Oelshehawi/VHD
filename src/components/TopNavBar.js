import * as React from "react";
import "../style/layout.css";

const TopNavBar = () => {
  return (
    <header className="HeaderBar" >
      TopNavBar
    </header>
  );
};

export default TopNavBar;
