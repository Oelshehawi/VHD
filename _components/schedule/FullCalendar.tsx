const FullCalendar = ({
  invoices,
  scheduledJobs,
  canManage,
}: {
  invoices: any;
  scheduledJobs: any;
  canManage: boolean;
}) => {
  return <div>FullCalendar</div>;
};

export default FullCalendar;
