// Backward compatibility - re-export everything from the new modular structure
// All existing imports like: import { Client } from "models/reactDataSchema" will continue to work
export * from "./index";
